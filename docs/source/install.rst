Installation guide
==================

************
Main package
************

The ``scCobra`` package can be installed via the following commands:

.. code-block:: bash
    :linenos:

    conda create -n scCobra python=3.9
    pip install scanpy torch scib
    git clone https://github.com/mcgilldinglab/scCobra.git
   

